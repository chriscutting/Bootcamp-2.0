prompt("What is your name?");
prompt("What is their name?");
var calcNumber=(Math.floor(Math.random() * 100 + 1));
console.log(calcNumber);
if (calcNumber < 30) {
alert("Oh no, that's a low score, you only got " + calcNumber);
}
else {
alert("Go for it. You scored a " + calcNumber);
};
